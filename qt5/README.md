# Qt5

Fmind light color scheme for Qt5 and Qt6 applications via `qt5ct` / `qt6ct`.

## Install

Copy `fmind.conf` to `~/.config/qt5ct/colors/` (or `~/.config/qt6ct/colors/`), then select Fmind in `qt5ct` or `qt6ct` under Appearance → Color Scheme.
