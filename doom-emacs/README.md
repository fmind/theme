# DOOM Emacs

Fmind light theme for DOOM Emacs.

## Install

Copy `doom-fmind-theme.el` to `~/.doom.d/themes/` (or `~/.config/doom/themes/`), then set your theme in `config.el`:

```elisp
(setq doom-theme 'doom-fmind)
```
