# Bashtop

Fmind theme for [Bashtop](https://github.com/aristocratos/bashtop).

## Install

1. Copy `fmind.theme` to `~/.config/bashtop/themes/`.
2. Select `fmind` in Bashtop menu (`Esc` -> `Options` -> `Color theme`).
