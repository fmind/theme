# Git

Fmind light theme for Git CLI output (`git diff`, `git status`, `git branch`).

## Install

Include `fmind.gitconfig` from your global `~/.gitconfig`:

```ini
[include]
	path = /path/to/git/fmind.gitconfig
```
