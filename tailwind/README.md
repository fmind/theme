# Tailwind

Fmind light theme color palette preset for Tailwind CSS.

## Install

In your `tailwind.config.js`:

```javascript
const fmindTheme = require("/path/to/tailwind/fmind.js");

module.exports = {
  presets: [fmindTheme],
  // ...
};
```
