# App coverage

Fmind provides 104 native integrations. The [free Dracula catalog](https://draculatheme.com/) listed 472 themes when reviewed on 2026-09-14. Its entries include applications, plugins, website styles, platforms, wallpapers and hardware; that count is not directly comparable to app folders. No Dracula Pro assets were used.

The [README](README.md) lists every shipped file and its installation instructions. Ports use Fmind’s light palette, with dark text, pale highlighted surfaces and bright accents. Dracula is the coverage reference. Notepad++ lexer definitions and Qt Creator editor definitions are adapted from MIT-licensed free Dracula ports, with their notices retained alongside the files; all ports use Fmind’s palette. WindTerm’s UI layout, icon selectors and native scope definitions are adapted from its MIT-licensed free Dracula port, with its notice retained in `windterm/LICENSE`.

The [472-entry catalog checklist](CATALOG.md) tracks the full requested scope, with shipped ports and explicit pending entries. This is the completion baseline; the areas below summarize it.

## Scope of the current ports

| Area | Coverage |
| --- | --- |
| Code editors | Vim, Neovim, Emacs, VS Code, Helix, Zed, Micro, Sublime Text, JetBrains, Kate, Gedit, Geany, Xcode, Notepad++, Qt Creator, TextMate, NetBeans, Eclipse, Code::Blocks, Dev-C++, Atom, Brackets and lualine. JetBrains and Sublime are editor color schemes, not complete application UI themes. |
| Notes and browsers | Obsidian and Typora; Chrome and Firefox interface themes. Browser themes do not restyle web pages. Firefox’s local temporary install ends at restart; permanent installation requires signing. |
| Terminals | Alacritty, Ghostty, Kitty, WezTerm, foot, Rio, Konsole, iTerm2, Windows Terminal, GNOME Terminal, Hyper, Terminator, Xfce4 Terminal, Mintty, Termux, Xresources, Terminal.app, Tilix, Warp, MobaXterm, ConEmu, QTerminal, LXTerminal, Termite, st, Fluent Terminal, WindTerm, COSMIC Terminal, Pantheon Terminal and tym. |
| Shells and terminal tools | Fish, PowerShell input highlighting, zsh-syntax-highlighting, Starship, tmux, Zellij, bat, delta, fzf, Atuin, bottom, btop, Fastfetch, gh-dash, k9s, Lazydocker, LazyGit, lsd, OpenCode, ptpython and Yazi. |
| Linux desktop | i3, bspwm and Hyprland window colors; Waybar, Polybar, Rofi, Wofi, Fuzzel, Dunst, SwayNotificationCenter, SwayOSD, WOB and Zathura; Swaylock and Hyprlock visual settings. Several are fragments for existing configurations. |
| Python tools | Streamlit application colors, Matplotlib plot styles, IDLE highlighting, a Pygments style, Spyder syntax settings and RStudio. |

KSyntaxHighlighting apps can use the Kate scheme, GtkSourceView apps can use the Gedit scheme, and Xresource-aware terminals can use Xresources. VS Code derivatives may accept the local extension, but they are not counted as separately tested integrations.

## Remaining work

Full Dracula parity is not claimed. These areas remain required work toward full catalog parity. The checklist also includes every remaining catalog entry:

| Area | Examples still missing | Work needed |
| --- | --- | --- |
| Other major IDEs | Visual Studio, Arduino IDE and Arduino Pro IDE | Native platform validation and language-specific coverage. |
| More terminals | NewTerm2, SecureCRT and mRemoteNG | Native profile formats and import validation. |
| Python and data IDEs | Broader runtime and platform checks for the shipped Jupyter, Thonny and MATLAB ports | Continue realistic notebook, debugger, output and platform validation. |
| Full desktop themes | GTK desktop-shell companions, legacy GTK 3, Qt and KDE | The GTK widget package is available; its [bundle checklist](gtk/README.md#full-bundle-checklist) keeps every remaining companion explicit. Broader desktop and application-specific testing remains required. |
| Productivity and communication | Slack, Telegram, Raycast, Alfred, Logseq, Thunderbird | App-specific import formats, supported customization boundaries and ongoing UI maintenance. |
| Creative and specialist tools | Blender, Godot, GIMP, Inkscape, KiCad | Domain-specific colors and representative native fixtures. |
| Websites and legacy integrations | Site-specific browser styles, discontinued editors | Native customization routes, selector coverage and legacy format validation. |

## Validation boundary

The repository gate checks native document parsing, palette use, terminal slot parity, selected text, filled controls and syntax contrast. Available local runtimes provide additional checks, including Vim/Neovim highlighting. The two synthetic screenshots exercise the existing terminal demonstration; they are not screenshots of every supported application.

The new ports have not all been launched in their target applications. Runtime changes, third-party plugins, transparency, user overrides and arbitrary ANSI background combinations remain outside the contrast claim. Setup and checks do not install themes into user profiles or call live application services.

The Terminal.app port has decoded NSColor archive checks, and Xcode has RGBA and font-field checks. IDLE is loaded through its installed Python parser, and Pygments renders real Python and diff samples. The new Windows/macOS desktop ports have no target-platform runtime proof yet. ConEmu uses BGR-encoded Windows slot ordering and reserves slot 15 as the white canvas; that one slot is not a readable foreground on white. Other terminal ANSI slots retain Fmind’s contrast contract.

Notepad++ includes 60 lexers and 842 syntax styles, with unique per-lexer IDs and a corrected Verilog line-comment collision from the reference port. Qt Creator defines all 441 color roles in the 20.0.1 native theme enum, plus 66 editor styles, with no external theme includes. Spyder targets the released 6.1 configuration format. RStudio CSS uses Ace and documented theme selectors, with the light Modern UI as its base. These ports have offline structural and contrast checks; target-app rendering and import validation remain outstanding.

Hyprland targets the current Lua configuration (0.55+, checked against the 0.56.2 example); its Hyprlock companion contains only visual widgets. Swaylock uses core color options without an effects fork. Fuzzel covers all twelve documented RGBA color roles, and dmenu includes all three native color schemes. SwayNotificationCenter targets 0.12.6 and imports the app’s installed layout, with Fmind native CSS variables and overrides for built-in notifications and widgets. Wofi parses without diagnostics in GTK 3.24.38, and SwayOSD in GTK 4.8.3. SwayNotificationCenter requires GTK 4.16.13 or newer, which is unavailable here; its full stylesheet has CSS syntax and contrast checks but no matching native-parser proof. The dmenu header compiles as C99 with warnings treated as errors against its native scheme enum. Live compositor, launcher, notification, lock-screen and panel rendering remain unverified.

The terminal batch includes all sixteen ANSI slots for nine ports; QTerminal and COSMIC also define dim colors. WindTerm includes 55 native styles and 50 syntax scope rules, plus its Qt UI stylesheet and icon color selectors. Pantheon’s custom palette and appearance keys are checked against the native settings schema. st reserves separate default background and cursor slots so ANSI white remains readable on the white canvas. The native extended 256-color palette and arbitrary applications’ background choices are outside the ANSI contrast contract. Target-app rendering and imports still require separate verification.

Additional offline evidence for this batch: COSMIC’s RON file deserializes with its native `ColorScheme` and `ColorSchemeAnsi` definitions and RON 0.11; Qt QSettings loads QTerminal’s color groups; GLib parses the VTE ports’ key files and Pantheon’s seven typed settings; and st’s header compiles warning-free as C99 and returns the intended default color indices. WindTerm’s QSS parses with Qt using asset loading disabled, and all 27 original asset paths are separately verified against its 2.7.0 release archive. This does not establish rendering in WindTerm’s custom widgets or its bundled Qt version.

The IDE batch adds NetBeans’ 27 MIME profiles and shared settings (549 color entries), Code::Blocks 25.03 coverage for all 62 native lexers (1,324 named style entries), Dev-C++ C/C++ syntax and editor state pairs, Atom’s syntax-theme package and public syntax variables, and a light Brackets CodeMirror theme. Eclipse includes 47 theme roles plus 382 native preference entries for core editors, JDT, CDT, Ant, the debug console and PyDev. NetBeans, Atom and Eclipse role definitions retain the free reference ports’ MIT notices. The NetBeans import excludes unrelated reference settings and obsolete user metadata.

All 30 NetBeans color documents also validate offline against its native Fonts and Colors 1.1 DTD. Code::Blocks’ stored style-name sets match all 62 lexers in the official 25.03 source archive, and Atom’s stylesheet compiles with Less 4.9.1. These checks do not run the target IDEs or Atom’s embedded Less version.

These are native editor/theme packages with structural, language-role and contrast checks. Light application appearance remains a separate setting where the application separates UI and syntax themes. Full target-IDE rendering, plugin interactions and native imports remain unverified; a passing format check does not establish those outcomes. Visual Studio and both Arduino catalog entries remain pending.

JupyterLab and Notebook 7 share a native Fmind extension with all 221 variables in JupyterLab 4.6.3’s native light-theme API. A separate classic Notebook/NbClassic stylesheet covers the dashboard, cells, syntax, output, forms and dialogs. Thonny adds 35 UI styles and 104 syntax/ANSI tags using its native plugin API and Enhanced Clam layouts. MATLAB includes the R2025a+ MathWorks JSON format with all 15 additional language sections and legacy Schemer preferences; the import excludes indentation policy.

The Jupyter wheel uses the current Jupyter builder and shares the host apputils service rather than bundling it. Its build dependency override pins sanitize-html 2.17.7 because apputils currently requires an affected older range; the built extension contains only the theme registration and CSS. This does not update or validate the host Jupyter application’s dependencies. The upstream exenv-es6 package remains deprecated in the build dependency graph.

Native checks for this batch: both Python wheels build and install in an isolated environment. JupyterLab 4.6.3 discovers the extension, selects Fmind, renders a white notebook canvas with Fmind syntax, and restores the native font when the theme unloads. Notebook 7.6.2 also selects Fmind through Settings → Theme and renders the white notebook canvas; its pre-existing startup errors are retained below. NbClassic 1.3.3 loads the separate stylesheet, with the syntax cascade verified in Chromium. Thonny 5.0.0 registers Fmind through its Workbench API; Tk 9.0.4 loads the inherited widget layouts, all 35 UI style overrides and all 104 syntax/ANSI tags. MATLAB’s 116 leaf preferences match the MathWorks template after omitting indentation settings. MATLAB import/rendering and full Thonny workbench/debugger behavior remain unverified.

The repository gate is warning-free, but the complete native campaign is not: Thonny 5.0.0 emits two Python 3.14 SyntaxWarnings from upstream `codeview.py` return statements inside finally blocks, and NbClassic uses a deprecated Jupyter Server extension-discovery function. Notebook 7.6.2 also reports startup errors (`Cannot read properties of undefined (reading schema)` and an unregistered `filebrowser:open-path` command) before selecting Fmind in this mixed Jupyter environment. These retained upstream/runtime gaps prevent a claim that the entire native campaign is green.

The GTK package adds native GTK 2, GTK 3.20+ and GTK 4 widget themes. It imports each modern toolkit's built-in layout and symbolic assets, then applies directly maintained Fmind colors; GTK 2 uses its built-in drawing engine. No third-party desktop stylesheet or binary assets are redistributed. The full Dracula GTK catalog row remains pending: legacy GTK 3, application-specific refinements, GNOME Shell, Cinnamon, KDE Plasma/Aurorae/Kvantum and Unity remain on the [bundle checklist](gtk/README.md#full-bundle-checklist).

Native GTK evidence on 2026-09-14: GTK 2.24.33 parses the RC file and returns all 80 expected foreground, background, text and base color values across four widgets and five native states. GTK 3.24.38 and GTK 4.8.3 parse the complete CSS imports without theme diagnostics and discover Fmind from a temporary home through `GTK_THEME=Fmind`. Both render an isolated widget gallery. GTK 3 passes foreground/background contrast checks for ten widgets across twelve state combinations (120 cases, including disabled/checked/backdrop combinations); GTK 4 passes the corresponding foreground palette and sampled rendered-background contrast checks for all 120 cases, with transparent surfaces composited on the white parent. The rendered gallery also covers action buttons, entries, selections, checks, switch, progress and scale. These samples do not establish contrast at every pixel or in every application widget. The gate also checks RC state pairs, CSS surface contrast, semantic/disabled action states, named GTK 3 colors and import closure. The two repository screenshots were refreshed; they remain terminal demonstrations, not GTK galleries.

GTK 4's host ChromeOS input-method module emits `Failed to initialize Wayland manager for GTK4 IM module` under the isolated X11 display, even when the application selects the simple input method. This retained host error prevents calling the complete GTK runtime campaign warning-free. Theme parsing and the stated widget checks pass; full application behavior, newer GTK releases, desktop-shell companions and libadwaita application styling remain unverified. No installed theme or desktop setting was modified during these checks.

The window-decoration batch adds a native Metacity version 3 XML theme and complete Xfwm assets in normal, HiDPI and extra-HiDPI sizes. Metacity uses independent vector drawing operations, with six window types, all focus/resize states, maximize/shade/tile combinations and eleven distinct control glyphs including restore. Xfwm has 60 XPM assets at each size (180 total): 24 frame pieces and 36 button-state images, with distinct toggle glyphs. Titles remain readable on white and button glyphs meet 4.5:1 on their state fills. Both themes retain the desktop's configurable button layout; Xfwm also retains its title-alignment setting.

Native evidence on 2026-09-14: Metacity 3.46.1 loads Fmind from the XDG data directory and renders 2,304 frame cases across six window types, eight states, both focus states, four resize-permission combinations, three button states and two scales, with warnings treated as fatal. An initial expression failure during painting was fixed by replacing unsupported unary-negative operands with binary subtraction; the regression check covers this parser/render distinction. A native gallery was visually inspected. The library's public renderer exposes menu, minimize, maximize and close; shade, above and stick control interaction in other engines remains unverified.

GdkPixbuf decodes all 180 XPM assets at their expected dimensions. Xfwm 4.18.0 runs with an isolated X server, D-Bus session, Xfconf configuration and temporary theme directories. All three variants load and decorate a synthetic client with clean window-manager and Xfconf logs; native frame extents confirm title heights of 30, 60 and 90 pixels with borders of 4, 8 and 12 pixels. A userspace path mapping supplies the packaged Xfwm defaults at its compiled lookup path without changing the host filesystem. Live compositor effects, every interactive button transition, Marco and other Metacity-compatible engines remain outside this runtime proof. No running desktop or installed theme was changed.

The Cinnamon companion adds native desktop colors and 13 independent SVG controls. It imports the installed Cinnamon base for geometry and applies Fmind rules to the component selectors used by the free reference and Cinnamon 5.6.8/6.6.9. Panels, menus, calendars, notifications, dialogs, workspaces, applets, desklets, keyboards, entries and Looking Glass are included. The [GTK bundle checklist](gtk/README.md#full-bundle-checklist) remains pending overall because other desktop companions and legacy widget work remain.

Native Cinnamon evidence on 2026-09-14: the St 5.6.8 engine parses the relocated Fmind import graph in an isolated Xvfb display with memory-only settings and warnings treated as fatal. It resolves 590 selector cases, including descendant chains and actual St widget types, against both the installed 5.6.8 base and the [stable 6.6.9 base](https://github.com/linuxmint/cinnamon/tree/6.6.9/data/theme) compiled with upstream's Sass route. All tested foreground/background pairs meet 4.5:1; the minimum is 5.09:1. Transparent text shadows are excluded, and transparent text nodes use their tested ancestor surface. The probe also exercises custom color lookups, borders, gradients and all 13 local background-image paths. GdkPixbuf decodes every SVG at its declared dimensions; the rendered control contact sheet was inspected. The gate retains import/asset closure, checked-state distinctions, opaque text surfaces and filled-control contrast checks.

This is native style resolution using St 5.6.8, not a rendered Cinnamon shell or a 6.6 runtime test. Direct private-Clutter initialization cannot start an input backend without Muffin's compositor setup; the successful node probe initializes only GTK's isolated icon screen and Cinnamon's settings schemas. Full desktop interaction, wallpaper composition, screen-locker behavior and third-party applet layouts remain unverified. The native dependency extraction, settings schemas and probes were confined to temporary directories; no installed theme or user settings were changed.

The GNOME Shell companion adds a shared native color layer, a GNOME 47+ entry using native checkbox/switch actors, and a GNOME 43–46 entry using image controls. The selector inventory covers the free Dracula shell/legacy styles and GNOME 43.9/50.4; the system default continues to supply layout. Eleven local SVG controls and placeholders are referenced, including eight reused from Cinnamon. No upstream stylesheet or artwork is redistributed. Installation uses the official User Themes extension and the existing [GTK package](gtk/README.md#gnome-shell).

Native GNOME evidence on 2026-09-14: St 43.9 with Mutter 43.8 runs in isolated Xvfb/D-Bus sessions with memory-only settings and GLib warnings treated as fatal. The final layer passes 2,361 selector cases against GNOME 43.9's resource stylesheet and 2,354 cases against each of GNOME 50.4's released light/dark stylesheets: 7,069 cases total. The latter stylesheets come from the checksum-verified [50.4 source release](https://download.gnome.org/sources/gnome-shell/50/). The minimum tested foreground/background contrast is 6.04:1. Transparent hidden glyphs and non-text capture masks are excluded; transparent text backgrounds use the tested ancestor surface. Native widget types are used for St selectors; the old reference's `Gjs_Arrow` name is represented by a registered test subtype for selector matching.

The native probe exercises border, gradient, padding, custom color and image lookups. St/Cogl paints 42 control background cases per base stylesheet, 126 total, and framebuffer center pixels equal the resolved backgrounds in every case. These are native control surfaces, not full application screenshots or rendered text. The rendered surface contact sheet was inspected, and GdkPixbuf decodes all 11 referenced SVGs at their declared sizes. The retained gate checks import/asset closure, imported-rule priority, St's last-matching-selector specificity, modern versus legacy controls, hidden selection markers, ordinary white surfaces, capture transparency and contrast.

The first direct St probe failed because Mutter's Clutter context was missing; initializing an isolated Mutter backend resolved that setup failure. Xvfb still emits `Xlib: extension "DPMS" missing` diagnostics during backend setup; those display-power diagnostics are retained as a native-environment limitation. The required repository gate is separate. No full shell session, GNOME 50 engine, intermediate release, third-party extension or lock-screen/GDM interaction is claimed as tested. Temporary source packages, native libraries, rendering fixtures and settings schemas were isolated and removed; no installed theme or user settings were changed.

The KDE batch adds an application color scheme and a Plasma desktop style inside the GTK bundle. Both contain all seven KDE color sets, plus inactive headers and window-title colors. Nine independent SVGs supply seven opaque surface types and explicit list/task states; other Plasma assets use the installed Breeze fallback. The expanded [KDE checklist](gtk/kde/README.md#bundle-checklist) also records the free reference's global themes, Plasma 5 session components, cursors and SDDM, alongside Aurorae and Kvantum. Those packages remain pending, so the full GTK catalog entry is not marked shipped.

Native KDE evidence on 2026-09-14: KDE Frameworks 5.103 with Qt 5.15.8 loads the package through `KColorScheme` and `Plasma::Theme` in isolated Xvfb/D-Bus sessions, with Qt warnings treated as fatal. The probe checks 945 text/background pairs across active, inactive and disabled application colors, generated semantic backgrounds, Plasma color groups and rendered controls. After rounding colors to display precision, minimum contrast is 4.66:1. The original amber text on KDE's generated neutral selection background failed at 4.32:1; the selected neutral role now uses the main text color. Disabled fading is limited to retain contrast without tinting ordinary backgrounds.

`Plasma::FrameSvg` renders 3,024 opaque surface cases across seven assets, nine prefixes, three device-pixel ratios and all 16 border combinations. Center pixels are opaque white, and complete borders have no slice seams. It also renders 306 custom list/task state cases and 135 inherited Breeze button, entry and tab cases. All rendered text/background pairs pass. Native probing exposed unreadable inherited list/task fills, so those assets are now supplied explicitly. The frame gallery was inspected with Google Sans labels. The retained offline gate checks RGB roles, contrast, package identity, effect settings, matching application/Plasma colors, SVG reference closure and state colors.

The first native setup lacked the KDE X11 platform plugin's `libxcb-res` dependency, then selected an unavailable default icon theme. Supplying the dependency and selecting installed Adwaita icons in the temporary configuration resolved both setup failures; the final native run has empty stderr. No installed themes or user settings were changed. Full Plasma sessions, Plasma 6/KSvg execution, third-party widgets, every inherited icon and arbitrary application widget styles remain outside this runtime proof. Downloaded native dependencies, test configuration, caches and render fixtures were confined to temporary directories and removed after verification.

The Aurorae batch adds a complete native SVG decoration package inside the KDE/GTK bundle: a configurable white frame, active/inactive title colors, maximized/opaque frame states and eleven button assets. Every button supplies eight active/inactive, hover, pressed and deactivated states. Desktop, above/below and shade toggles retain a visible pressed fill; shade reverses its arrow and maximize has a separate restore glyph. Application-menu and window-menu SVGs are supplied for engines that support them; KWin 5.27 uses its native icon for those controls. The package contains original Fmind geometry and follows the [native Aurorae format](https://develop.kde.org/docs/plasma/aurorae/).

Native Aurorae evidence on 2026-09-14: the unmodified `AuroraeTheme` and `ThemeConfig` sources from KWin 5.27.5 were compiled against KDE Frameworks 5.103 and Qt 5.15.8. The loader discovers Fmind in an isolated data directory, resolves all nine SVG button types supported by that loader and returns the expected title colors. All 100 combinations of its ten border-size and ten button-size settings keep controls inside the normal/maximized title area, with maximized side/top margins at zero. One pixel below maximized controls prevents fractional button sizes from being clipped by Aurorae's integer border calculation.

`Plasma::FrameSvg` renders 612 distinct decoration cases across normal/inactive/opaque/maximized prefixes, three frame sizes, three device-pixel ratios and supported border combinations. It also renders 1,320 button cases across eleven assets, eight states, five sizes from 14 to 56 pixels and device-pixel ratios 1, 1.5 and 2. Background pixels match the intended fills, every button is opaque, and every glyph retains visible ink at small sizes. The 1,322 configured title/glyph contrast checks have a minimum of 5.69:1; antialiased edge pixels are not treated as solid foreground colors. A native frame/button gallery was visually inspected. Initial probing caught faint close/minimize glyphs at small sizes and maximized size-rounding clipping; both are corrected. Final native loader/render stderr is empty with Qt warnings fatal. The retained offline gate covers package identity, SVG closure, full button states, toggle visibility, ordinary white surfaces, title/glyph contrast and layout constraints.

A separate full KWin/X11 attempt in disposable Xvfb/D-Bus sessions remains unverified. Setup first exposed an absent Plasma default-data package, Xvfb's invalid refresh rate, unsupported media-key names, missing shortcut-daemon activation and an empty initial tiling configuration. Supplying the isolated dependencies, a 60 Hz test display, basic US keyboard rules, the shortcut daemon and valid empty tiling data resolved those setup issues. KWin then reached decoration loading but Qt reported `Module 'org.kde.kwin.decoration' does not contain a module identifier directive`, despite the packaged `qmldir` containing it, and the warnings-fatal process stopped. No complete window-session or button-interaction pass is claimed; Plasma 6/KSvg is also unverified. The theme's successful native loader/render checks are separate from this failure. Temporary processes, libraries, configs, caches, sources and render fixtures were removed after verification; installed themes and user settings were untouched.

The Kvantum batch adds `Fmind.kvconfig`, `Fmind.svg` and the matching KDE `Fmind.colors` under [gtk/kde/kvantum/Fmind](gtk/kde/kvantum/Fmind/). The native configuration covers 43 widget sections, including toolbar variants, dialogs, dock panels, MDI controls, dials, item views and progress bars. Shared SVG geometry keeps the controls consistent while explicit checkbox/radio/arrow/MDI states preserve their meaning. The free reference's four Kvantum color/transparency variants are represented by one opaque Fmind light theme. Kvantum is implemented, while the full KDE/GTK companion checklist remains pending.

Native Kvantum evidence on 2026-09-14: the Kvantum 1.0.7 style plugin runs with Qt 5.15.8 in disposable Xvfb displays. Six cases cover scale factors 1, 1.5 and 2 in left-to-right and right-to-left layouts, with Qt warnings fatal. Each case checks 51 palette/surface text pairs, paints 60 control-state cases and performs seven widget state checks (checkbox changes, radio selection, combo selection, spin increment, tab switch and MDI restore). All 306 pairs pass, with minimum contrast 6.63:1; all 360 state renders and 42 widget state checks pass. These state changes use Qt widget APIs, not a claim of complete mouse/keyboard interaction testing. Every one of the 431 SVG elements also has valid native bounds and paints visible pixels through `QSvgRenderer`.

The native gallery includes normal/default/disabled buttons, checks/radios, selected and disabled text inputs, editable combos, spin/date controls, tables/trees, tabs, groups, horizontal/vertical sliders and scrollbars, progress bars, dials, toolboxes, splitters, dock panels and MDI windows. Popup menus and tooltips were captured separately. Visual review included the ordinary widget gallery, MDI/dock/vertical controls, menus/tooltips and a 150% right-to-left layout. A temporary SVG-call tracer records 7,537 widget rendering calls across the final six cases, all from Fmind's SVG; none draw fallback SVG artwork. This traces the exercised paths, not every possible application widget or non-SVG Qt drawing operation.

Native probing found three theme defects: Qt used the palette's Dark role for an ordinary gray MDI workspace, disabled progress lost its fill, and a menu separator stretched into a thick bar. The Dark role is now white, disabled progress has an explicit fill, and separator artwork has transparent bounds around a thin rule. The retained offline gate checks palette contrast, ordinary/MDI backgrounds, the KDE color-scheme copy, configuration inheritance, native asset closure, indicator/check states, disabled progress and opaque effects. Final native stderr is empty. Initial test setup required GL headers and the plugin's WindowSystem library; an SVG tracer initially failed because its linker had dropped the QtSvg dependency, which was corrected before using its results.

Qt 6, current Kvantum's Qt 6 engine, full KDE/LXQt sessions, application-specific custom painting and Qt Quick controls remain outside this runtime proof. The native plugin, headers, tracing helper, probes, configuration, caches and render fixtures were confined to temporary directories and removed after verification. Installed themes and user settings were not changed.

The global-theme batch adds a native Plasma look-and-feel package with Plasma 5 desktop metadata, Plasma 6 JSON metadata, component defaults, a complete color-scheme copy and a Qt Quick startup splash. Defaults reference the shipped KDE colors, Plasma style, Aurorae decorations, Kvantum engine and Google Sans/Google Sans Code fonts. Kvantum Manager still selects its active Fmind theme separately. No layout, wallpaper, cursor, icon or online package dependency is included. The GTK catalog entry remains pending for its other companion packages.

Native look-and-feel evidence on 2026-09-14: KDE Frameworks 5.103 discovers Fmind through `kpackagetool5`; 24 checks using KPackage, KConfig and QImage pass for package identity, component paths/defaults, serialized fonts and both previews. Plasma 5.27.5's `ksplashqml --test --window --nofork Fmind` completes in 12.35 seconds with exit 0 and empty output under disposable D-Bus and Xvfb sessions. Configuration, data, cache and runtime directories are isolated; no global theme is applied to the user's session.

The splash renders in Qt 5.15.8 and Qt 6.4.2 at 100%, 150% and 200% scaling. Each of the six engine/scale combinations covers six logical sizes (240×160, 320×240, 960×540, 1920×1080, 1080×1920 and 2560×1080) and ten stage inputs, including negative and out-of-range values. All 360 renders and 7,242 assertions pass with warnings fatal: white background, visible content within the viewport, readable unclipped text, font serialization and exactly one activity marker. Minimum text contrast is 6.63:1. The package preview is the 960×540 Qt 6 rendering; its splash-preview entry links to the same image.

Initial probe failures were in the fixtures: QObject lookup did not find Repeater's visual children and caused a null dereference; traversal of QQuickItem children fixed it. The standalone KConfig font probe initially lost KConfigGui to the linker's unused-library removal; retaining that library registered its QFont reader. Both corrected probes pass without suppressing warnings. These checks establish native loading in Plasma 5 and QML rendering in both Qt generations, not application of a global theme to a full desktop, a Plasma 6 KPackage load or its native KSplash lifecycle. Existing component-specific runtime limits still apply.

The required screenshot refresh initially missed its final PNG: [VHS 0.11.0 schedules `Screenshot` for the next frame](https://github.com/charmbracelet/vhs/blob/v0.11.0/vhs.go#L406-L412), and the tape ended immediately afterward. A final 500 ms recording interval lets that frame arrive. The capture script now verifies that both images exist before copying either retained image. The palette scanner also distinguishes binary PNG previews from native text; package checks verify PNG headers, dimensions and internal paths while the QML source retains the normal palette check.

Final repository validation passes: `mise install`, `mise run install`, `mise run format`, all 123 tests in `mise run check`, and `mise run screenshots`. Both refreshed synthetic images match the retained files.
