# Marp

Fmind light theme for Marp markdown slide decks.

## Installation

1. Reference `fmind.css` in your Marp markdown deck:
   ```markdown
   ---
   marp: true
   theme: fmind
   ---
   ```
2. Or pass `--theme /path/to/marp/fmind.css` to Marp CLI.
