# Midnight Commander

Fmind light skin for Midnight Commander (`mc`).

## Installation

1. Copy `fmind.ini` to `~/.local/share/mc/skins/fmind.ini` (or `~/.config/mc/skins/fmind.ini`).
2. In `~/.config/mc/ini`, set `skin=fmind` under `[Midnight-Commander]`.
3. Launch `mc`.
