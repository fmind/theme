# Microsoft Edge

Fmind light theme extension for Microsoft Edge.

## Installation

1. Open Microsoft Edge and navigate to `edge://extensions`.
2. Enable **Developer mode** toggle.
3. Click **Load unpacked** and select the `microsoft-edge/` directory.
