# Gamepad Viewer

Fmind light theme for Gamepad Viewer (gamepadviewer.com).

## Installation

1. Host `fmind.css` or copy its content.
2. In the Gamepad Viewer URL generator, add the CSS URL or apply via browser extension / OBS browser source custom CSS.
