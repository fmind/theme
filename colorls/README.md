# colorls

Fmind light color theme for `colorls`.

## Install

Copy `fmind.yaml` to `~/.config/colorls/fmind.yaml` (or `~/.config/colorls/dark_colors.yaml`), or pass `--theme /path/to/fmind.yaml` to `colorls`.
