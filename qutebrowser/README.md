# Qutebrowser

Fmind light theme for Qutebrowser.

## Installation

1. Copy `fmind.py` to `~/.config/qutebrowser/fmind.py`.
2. In `~/.config/qutebrowser/config.py`, add:
   ```python
   config.source("fmind.py")
   ```
3. Restart Qutebrowser or run `:config-source`.
