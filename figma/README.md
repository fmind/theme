# Figma

Fmind color tokens for Figma.

## Installation

1. Install the [Tokens Studio for Figma](https://tokens.studio/) plugin (or any DTCG-compliant design tokens plugin).
2. Import `tokens.json` to load the Fmind color styles into your Figma document.
