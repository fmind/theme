# Fmind for vis

Light theme for [vis](https://github.com/martanne/vis).

## Installation

Copy [fmind.lua](fmind.lua) to `~/.config/vis/themes/fmind.lua` and set `theme = fmind` in your `visrc.lua`.
