# Linux TTY

Fmind color scheme for Linux virtual consoles (TTY).

## Install

Run `fmind-tty.sh` directly in a virtual terminal or source from your shell initialization (`~/.bashrc`, `~/.zshrc`) when `$TERM = "linux"`.
