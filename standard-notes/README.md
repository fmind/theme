# Standard Notes

Fmind theme extension for [Standard Notes](https://standardnotes.com/).

## Install

1. In Standard Notes, click `Extensions` in the bottom left.
2. Click `Import Extension` and paste the URL to `ext.json` (or import locally).
