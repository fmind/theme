# Cli-Visualizer

Fmind theme for [cli-visualizer](https://github.com/dpayne/cli-visualizer).

## Install

Copy `fmind.vis` to `~/.config/vis/colors/fmind`. Set `colors.scheme=fmind` in `~/.config/vis/config`.
