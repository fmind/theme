# Ulauncher

Fmind light theme for Ulauncher application launcher.

## Install

Copy the `ulauncher` directory to `~/.config/ulauncher/user-themes/fmind` and select Fmind in Ulauncher Preferences → Theme.
