# Mutt

Fmind light colors for Mutt and NeoMutt email clients.

## Installation

1. Copy `fmind.muttrc` to `~/.mutt/fmind.muttrc` (or `~/.config/neomutt/fmind.muttrc`).
2. Source it in your `muttrc`:
   ```muttrc
   source ~/.mutt/fmind.muttrc
   ```
