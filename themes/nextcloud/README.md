# Nextcloud

Fmind theme for [Nextcloud](https://nextcloud.com/).

## Install

1. In Nextcloud, install the **Custom CSS** app from the app store.
2. In Administration Settings -> `Theming` -> `Custom CSS`, paste the contents of `theme.css`.
