# Total Commander

Fmind light theme for Total Commander.

## Install

Merge the `[Colors]` section into your `wincmd.ini` (typically located in `%APPDATA%\GHISLER\wincmd.ini` or the Total Commander install directory).
