# Textual

Fmind theme for [Textual](https://www.textualirc.com/) on macOS.

## Install

1. Copy `design.css` to `~/Library/Application Support/Textual/Styles/Fmind/`.
2. Select **Fmind** in Textual Preferences (`Preferences` -> `Style`).
