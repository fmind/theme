# Fmind Powerlevel10k

A high-contrast light theme configuration for Powerlevel10k prompt.

## Install

1. Place [p10k-fmind.zsh](p10k-fmind.zsh) in your home directory or zsh configuration folder:
   ```sh
   cp p10k-fmind.zsh ~/.p10k-fmind.zsh
   ```
2. In your `~/.p10k.zsh` (or `~/.zshrc` after loading powerlevel10k), add:
   ```zsh
   [[ -f ~/.p10k-fmind.zsh ]] && source ~/.p10k-fmind.zsh
   ```
3. Restart your shell or run `source ~/.zshrc`.
