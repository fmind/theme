# dircolors

Fmind theme for [dircolors](https://www.gnu.org/software/coreutils/).

## Install

Add to your shell configuration (`~/.bashrc` or `~/.zshrc`):

```sh
eval "$(dircolors -b path/to/dircolors/.dircolors)"
```
