# Fmind for lnav

Light theme for [lnav](https://lnav.org/).

## Installation

Copy [fmind.json](fmind.json) to `~/.lnav/formats/installed/fmind.json` and run `:config /ui/theme fmind-theme` in lnav.
