# Fmind for GNU grep

Light color scheme for GNU grep.

## Installation

Source [fmind.sh](fmind.sh) or add the export to your `~/.bashrc`, `~/.zshrc`, or `~/.config/fish/config.fish`:

```sh
export GREP_COLORS='ms=01;31:mc=01;31:sl=:cx=:fn=34:ln=32:bn=32:se=36'
```
