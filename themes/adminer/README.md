# Adminer

Fmind theme for [Adminer](https://www.adminer.org/).

## Install

1. Copy `adminer.css` into the same directory as your `adminer.php` script.
2. Adminer will automatically load `adminer.css`.
