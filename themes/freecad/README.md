# FreeCAD

Fmind theme for [FreeCAD](https://www.freecad.org/).

## Install

1. Copy `Fmind.qss` to FreeCAD's stylesheet directory (`~/.local/share/FreeCAD/Gui/Stylesheets/` on Linux).
2. Open FreeCAD Preferences (`Edit` -> `Preferences` -> `General` -> `Style sheet`).
3. Select **Fmind**.
