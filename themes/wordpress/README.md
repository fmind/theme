# WordPress

Fmind light admin color scheme for WordPress.

## Install

Enqueue `fmind.css` via `wp_admin_css_color()` in your theme's `functions.php` or custom plugin, and select Fmind under Users → Profile → Admin Color Scheme.
