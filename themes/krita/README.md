# Krita

Fmind color scheme for [Krita](https://krita.org/).

## Install

1. Copy `Fmind.colors` to `~/.local/share/color-schemes/` (or `%APPDATA%\color-schemes\`).
2. In Krita, go to `Settings` -> `Color Themes` -> `Fmind`.
