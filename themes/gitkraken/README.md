# GitKraken

Fmind theme for [GitKraken](https://www.gitkraken.com/).

## Install

1. Copy `fmind-theme.jsonc` to `~/.gitkraken/themes/` (or `%APPDATA%\.gitkraken\themes\` on Windows).
2. In GitKraken, open **Preferences** > **UI Customization** > **Theme** and select **Fmind**.
