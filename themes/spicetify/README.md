# Spicetify

Fmind theme for [Spicetify](https://spicetify.app/).

## Install

1. Copy `color.ini` into your Spicetify theme folder (`~/.config/spicetify/Themes/Fmind/`).
2. Run:
```sh
spicetify config current_theme Fmind
spicetify apply
```
