# Fmind for man pages

Light theme highlighting for manual pages rendered with `less`.

## Installation

Source [fmind.sh](fmind.sh) in your `~/.bashrc` or `~/.zshrc`:

```sh
source /path/to/man-pages/fmind.sh
```
