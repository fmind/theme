# Spacemacs

Fmind theme for [Spacemacs](https://www.spacemacs.org/).

## Install

1. Copy `fmind-theme.el` to `~/.emacs.d/private/local/fmind-theme/`.
2. Add `fmind` to `dotspacemacs-themes` in `~/.spacemacs`.
