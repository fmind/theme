# MetaEditor

Fmind theme for [MetaEditor](https://www.metatrader5.com/en/automated-trading/metaeditor).

## Install

1. Open your MetaTrader configuration folder (`config/`).
2. Add the `[Colors]` section from `metaeditor.ini` into `metaeditor.ini`.
3. Restart MetaEditor.
