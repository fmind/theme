# https://docs.astral.sh/uv/concepts/projects/config/
[project]
name = "fmind-theme"
version = "3.0.1"
requires-python = ">=3.14"

[dependency-groups]
dev = [
  "pyyaml>=6.0.3",
  "markdown>=3.10.3",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 120
target-version = "py314"
