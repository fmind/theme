# Spotify TUI

Fmind light theme for Spotify TUI (`spt`).

## Installation

1. Merge the `theme` block from `fmind.yml` into your `~/.config/spotify-tui/config.yml`.
2. Launch `spt` in a terminal using the Fmind light color palette.
