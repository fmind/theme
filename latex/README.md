# LaTeX

Fmind theme package for LaTeX.

## Install

1. Copy `fmindtheme.sty` to your LaTeX project directory or local `texmf` tree.
2. In your document preamble, add:

```latex
\usepackage{fmindtheme}
```
