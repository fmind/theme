# KiCad

Fmind light color theme for KiCad schematics.

## Install

Copy `fmind.json` to `~/.config/kicad/8.0/colors/` (or your KiCad version directory) and select Fmind in Preferences → Schematic Editor → Colors.
